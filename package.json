{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "bootstrap": "lerna bootstrap --hoist"
  }
}
